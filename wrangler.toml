name = "idyab"
main = "index.js"
compatibility_date = "2026-08-01"

# اتصال به دیتابیس D1 (منبع اصلی داده)
[[d1_databases]]
binding = "DB"
database_name = "idyab-db"
database_id = "{807aeb3b-027e-4d26-966c-96bff28fb709}"

# متغیرهای غیرحساس (فقط برای وب‌هوک)
[vars]
WEBHOOK_SECRET = "{WEBHOOK_SECRET}"